# Event Catalog v1
#
# All domain events emitted by the system.
# Rules:
# - Events are immutable once shipped. Add new version instead of changing existing.
# - Every event payload MUST include: eventId, occurredAt, version, data
# - Naming: entity.action, lowercase with dots as separators
# - Add entry here BEFORE implementing event emission in code

version: "1"

standardPayload:
  required: [eventId, occurredAt, version, data]
  properties:
    eventId: { type: string, format: uuid }
    occurredAt: { type: string, format: date-time }
    version: { type: integer }
    data: { type: object }

events:
  customer.created:
    description: Fired when a customer record is created.
    version: 1
    owner: customers-module
    data:
      required: [customerId, actorUserId, name, customerKind]
      properties:
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        name: { type: string }
        customerKind: { type: string, enum: [company, person] }

  customer.updated:
    description: Fired when customer core fields change.
    version: 1
    owner: customers-module
    data:
      required: [customerId, actorUserId, changedFields]
      properties:
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  customer.status_changed:
    description: Fired when customer lifecycle status changes.
    version: 1
    owner: customers-module
    data:
      required: [customerId, actorUserId, fromStatus, toStatus]
      properties:
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        fromStatus: { type: string }
        toStatus: { type: string }

  customer.archived:
    description: Fired when a customer is archived.
    version: 1
    owner: customers-module
    data:
      required: [customerId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        archiveReason: { type: string }

  customer.restored:
    description: Fired when an archived customer is restored.
    version: 1
    owner: customers-module
    data:
      required: [customerId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.contact_created:
    description: Fired when a contact is created for a customer.
    version: 1
    owner: customers-module
    data:
      required: [customerId, contactId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        contactId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.contact_updated:
    description: Fired when a customer contact changes.
    version: 1
    owner: customers-module
    data:
      required: [customerId, contactId, actorUserId, changedFields]
      properties:
        customerId: { type: string, format: uuid }
        contactId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  customer.contact_archived:
    description: Fired when a customer contact is archived.
    version: 1
    owner: customers-module
    data:
      required: [customerId, contactId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        contactId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.primary_contact_changed:
    description: Fired when a customer's primary contact changes.
    version: 1
    owner: customers-module
    data:
      required: [customerId, contactId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        contactId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.billing_contact_changed:
    description: Fired when a customer's billing contact changes.
    version: 1
    owner: customers-module
    data:
      required: [customerId, contactId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        contactId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.address_created:
    description: Fired when an address is created for a customer.
    version: 1
    owner: customers-module
    data:
      required: [customerId, addressId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        addressId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.address_updated:
    description: Fired when a customer address changes.
    version: 1
    owner: customers-module
    data:
      required: [customerId, addressId, actorUserId, changedFields]
      properties:
        customerId: { type: string, format: uuid }
        addressId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  customer.address_archived:
    description: Fired when a customer address is archived.
    version: 1
    owner: customers-module
    data:
      required: [customerId, addressId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        addressId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.billing_address_changed:
    description: Fired when a customer's billing address changes.
    version: 1
    owner: customers-module
    data:
      required: [customerId, addressId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        addressId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.note_created:
    description: Fired when a note is created for a customer.
    version: 1
    owner: customers-module
    data:
      required: [customerId, noteId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        noteId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.note_updated:
    description: Fired when a customer note changes.
    version: 1
    owner: customers-module
    data:
      required: [customerId, noteId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        noteId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  customer.note_archived:
    description: Fired when a customer note is archived.
    version: 1
    owner: customers-module
    data:
      required: [customerId, noteId, actorUserId]
      properties:
        customerId: { type: string, format: uuid }
        noteId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  project.created:
    description: Fired when a project record is created.
    version: 1
    owner: projects-module
    data:
      required: [projectId, customerId, actorUserId, title]
      properties:
        projectId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string }
        title: { type: string }

  project.updated:
    description: Fired when project core fields change.
    version: 1
    owner: projects-module
    data:
      required: [projectId, actorUserId, changedFields]
      properties:
        projectId: { type: string, format: uuid }
        actorUserId: { type: string }
        changedFields:
          type: array
          items: { type: string }

  project.archived:
    description: Fired when a project is archived directly or by customer archive cascade.
    version: 1
    owner: projects-module
    data:
      required: [projectId, customerId, actorUserId]
      properties:
        projectId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string }

  project.status_changed:
    description: Fired when project lifecycle status changes.
    version: 1
    owner: projects-module
    data:
      required: [projectId, actorUserId, fromStatus, toStatus]
      properties:
        projectId: { type: string, format: uuid }
        actorUserId: { type: string }
        fromStatus: { type: string }
        toStatus: { type: string }

  quote.created:
    description: Fired when a quote record is created.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.updated:
    description: Fired when quote header fields change.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, customerId, actorUserId, changedFields]
      properties:
        quoteId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  quote.sent:
    description: Fired when a quote is sent.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.accepted:
    description: Fired when a quote is accepted.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.rejected:
    description: Fired when a quote is rejected.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.archived:
    description: Fired when a quote is archived.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.line_item_added:
    description: Fired when a line item is added to a quote.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, lineItemId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        lineItemId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.line_item_updated:
    description: Fired when a quote line item changes.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, lineItemId, customerId, actorUserId, changedFields]
      properties:
        quoteId: { type: string, format: uuid }
        lineItemId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  quote.line_item_removed:
    description: Fired when a quote line item is removed.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, lineItemId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        lineItemId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.area_added:
    description: Fired when a quote area is added to a quote.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.area_updated:
    description: Fired when a quote area changes.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, customerId, actorUserId, changedFields]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  quote.area_removed:
    description: Fired when a quote area is removed.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.counter_piece_added:
    description: Fired when a counter piece is added to a quote area.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.counter_piece_updated:
    description: Fired when a counter piece changes.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId, changedFields]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  quote.counter_piece_removed:
    description: Fired when a counter piece is removed.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.edge_segment_added:
    description: Fired when an edge segment is added to a quote area.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.edge_segment_updated:
    description: Fired when an edge segment changes.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId, changedFields]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  quote.edge_segment_removed:
    description: Fired when an edge segment is removed.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.sink_cutout_added:
    description: Fired when a sink cutout is added to a quote area.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  quote.sink_cutout_updated:
    description: Fired when a sink cutout changes.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId, changedFields]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  quote.sink_cutout_removed:
    description: Fired when a sink cutout is removed.
    version: 1
    owner: quotes-module
    data:
      required: [quoteId, areaId, measurementId, customerId, actorUserId]
      properties:
        quoteId: { type: string, format: uuid }
        areaId: { type: string, format: uuid }
        measurementId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  scheduled_event.created:
    description: Fired when a scheduled event is created.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  scheduled_event.updated:
    description: Fired when scheduled event fields change.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId, changedFields]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  scheduled_event.confirmed:
    description: Fired when a scheduled event transitions to confirmed.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  scheduled_event.started:
    description: Fired when a scheduled event transitions to in_progress.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  scheduled_event.completed:
    description: Fired when a scheduled event transitions to completed.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  scheduled_event.cancelled:
    description: Fired when a scheduled event transitions to cancelled.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  scheduled_event.rescheduled:
    description: Fired when a scheduled event scheduledAt value changes.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId, previousScheduledAt, newScheduledAt]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        previousScheduledAt: { type: string, format: date-time }
        newScheduledAt: { type: string, format: date-time }

  scheduled_event.archived:
    description: Fired when a scheduled event is archived.
    version: 1
    owner: scheduling-module
    data:
      required: [scheduledEventId, customerId, actorUserId]
      properties:
        scheduledEventId: { type: string, format: uuid }
        customerId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  slab.created:
    description: Fired when a slab record is created.
    version: 1
    owner: inventory-module
    data:
      required: [slabId, actorUserId]
      properties:
        slabId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  slab.updated:
    description: Fired when slab fields change.
    version: 1
    owner: inventory-module
    data:
      required: [slabId, actorUserId, changedFields]
      properties:
        slabId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  slab.reserved:
    description: Fired when a slab is reserved by a quote or project.
    version: 1
    owner: inventory-module
    data:
      required: [slabId, actorUserId]
      properties:
        slabId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        quoteId: { type: string, format: uuid }
        projectId: { type: string, format: uuid }

  slab.released:
    description: Fired when a reserved slab is released.
    version: 1
    owner: inventory-module
    data:
      required: [slabId, actorUserId]
      properties:
        slabId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        quoteId: { type: string, format: uuid }
        projectId: { type: string, format: uuid }

  slab.cut:
    description: Fired when a slab is cut and optional remnants are produced.
    version: 1
    owner: inventory-module
    data:
      required: [slabId, actorUserId, remnantSlabIds]
      properties:
        slabId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        remnantSlabIds:
          type: array
          items: { type: string, format: uuid }

  slab.archived:
    description: Fired when a slab is archived.
    version: 1
    owner: inventory-module
    data:
      required: [slabId, actorUserId]
      properties:
        slabId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  activity_type.created:
    description: Fired when a shop creates an Activity Type in its catalog (including seeding).
    version: 1
    owner: activity-types-module
    data:
      required: [activityTypeId, shopId, actorUserId, name]
      properties:
        activityTypeId: { type: string, format: uuid }
        shopId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        name: { type: string }

  activity_type.updated:
    description: Fired when an Activity Type's name, color, or behavior flags change.
    version: 1
    owner: activity-types-module
    data:
      required: [activityTypeId, shopId, actorUserId, changedFields]
      properties:
        activityTypeId: { type: string, format: uuid }
        shopId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        changedFields:
          type: array
          items: { type: string }

  activity_type.archived:
    description: Fired when an Activity Type is retired from the shop catalog.
    version: 1
    owner: activity-types-module
    data:
      required: [activityTypeId, shopId, actorUserId]
      properties:
        activityTypeId: { type: string, format: uuid }
        shopId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }

  shop.work_days_updated:
    description: Fired when a shop changes its Work Days set.
    version: 1
    owner: shop-settings-module
    data:
      required: [shopId, actorUserId, workDays]
      properties:
        shopId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        workDays:
          type: array
          items: { type: integer, minimum: 0, maximum: 6 }

  holiday.created:
    description: Fired when a shop adds a Holiday to its calendar settings.
    version: 1
    owner: shop-settings-module
    data:
      required: [holidayId, shopId, actorUserId, holidayDate, name]
      properties:
        holidayId: { type: string, format: uuid }
        shopId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        holidayDate: { type: string, format: date }
        name: { type: string }

  holiday.deleted:
    description: Fired when a shop removes a Holiday from its calendar settings.
    version: 1
    owner: shop-settings-module
    data:
      required: [holidayId, shopId, actorUserId, holidayDate]
      properties:
        holidayId: { type: string, format: uuid }
        shopId: { type: string, format: uuid }
        actorUserId: { type: string, format: uuid }
        holidayDate: { type: string, format: date }
