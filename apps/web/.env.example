# API
API_BASE_URL=http://localhost:3001/api/v1

# Better Auth (must match apps/api BETTER_AUTH_SECRET)
BETTER_AUTH_SECRET=change-me-to-a-random-32-char-string

# Database (Better Auth reads this directly in Next.js for session management)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/stoneboyz_crm

# Public URL (used for share links, emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000
